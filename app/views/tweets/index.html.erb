<section id="tweets">
  <h3>Your Timeline<%= link_to "Get Latest", update_path %></h3>
  <% if @tweets.any? %>
    <h4>Tweets <span><%= @tweets.count %></span></h4>
    <ul id="tweets">
      <%= render @tweets %>
    </ul>
    <%= will_paginate @tweets %>
  <% end %>
</section>
